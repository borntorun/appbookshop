/*'use strict';

var express = require('express');
var router = express.Router();
var publisherCtrl = require('./publisher.controller.js');

router.get('/search/:filter?/:limit?', publisherCtrl.search);

module.exports = require('../apirouter')(router, publisherCtrl);*/
